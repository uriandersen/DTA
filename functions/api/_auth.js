const enc=new TextEncoder();
export async function sha256(s){const b=await crypto.subtle.digest("SHA-256",enc.encode(s));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,"0")).join("")}
async function sig(payload,secret){const k=await crypto.subtle.importKey("raw",enc.encode(secret),{name:"HMAC",hash:"SHA-256"},false,["sign"]);const b=await crypto.subtle.sign("HMAC",k,enc.encode(payload));return btoa(String.fromCharCode(...new Uint8Array(b))).replace(/=+$/,"")}
export async function makeSession(secret,ttl=43200){const exp=Math.floor(Date.now()/1000)+ttl;const p="dta."+exp;return p+"."+await sig(p,secret)}
export async function validSession(request,secret){if(!secret)return false;const m=(request.headers.get("cookie")||"").match(/(?:^|;\s*)dta_access=([^;]+)/);if(!m)return false;const [a,e,s]=m[1].split(".");if(a!=="dta"||!e||!s||Number(e)<Date.now()/1000)return false;return s===await sig(a+"."+e,secret)}
export async function gate(context){const kv=context.env.DTA_ACCESS;if(!kv)return {ok:false,error:"Access store not configured"};const enabled=await kv.get("enabled");if(enabled!=="1")return {ok:false,error:"DTA is closed"};const secret=context.env.DTA_SESSION_SECRET;if(await validSession(context.request,secret))return {ok:true};return {ok:false,error:"Access code required"}}
export function json(data,status=200,headers={}){return new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json; charset=utf-8",...headers}})}
